[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers

# The scripts under src/test_*.py are standalone offline harnesses. They load
# optional, explicitly selected ground-truth folders and intentionally call
# sys.exit; keeping collection in tests/ makes `python -m pytest -q` safe.
